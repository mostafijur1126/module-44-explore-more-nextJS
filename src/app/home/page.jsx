import { Button } from '@heroui/react';
import React from 'react';

const page = () => {
    return (
        <div>
            <h1>hero page</h1>
            <Button>hero button</Button>
        </div>
    );
};

export default page;